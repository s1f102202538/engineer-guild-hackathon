name: _install-yarn-deps

on:
  workflow_call:
    inputs:
      use-yarn-cache:
        description: 'Use yarn cache'
        type: boolean
        required: false
        default: true

jobs:
  cache-and-install:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      yarn-cache-path: ''
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: ./.github/actions/common-setup

      - name: Setup yarn v4
        run: |
          corepack enable
          yarn set version 4.6.0

      - name: Restore yarn cache
        uses: ./.github/actions/restore-yarn-cache
        id: restore-yarn-cache
        if: ${{ inputs.use-yarn-cache }} == true

      - name: Install dependencies
        run: yarn install
        if: steps.restore-yarn-cache.outputs.cache-hit != 'true'

      - name: Get yarn dependencies from cache
        run: |
          echo "YAR_CACHE_DIR=$(yarn config get cacheFolder)" >> $GITHUB_ENV

      - name: Save yarn cache
        uses: actions/cache/save@v4
        if: steps.restore-yarn-cache.outputs.cache-hit != 'true'
        with:
          path: ${{ env.YAR_CACHE_DIR }}
          key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
